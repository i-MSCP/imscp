sudo: false

language: perl

addons:
  apt:
    packages:
    - fakeroot

install:
    - true # bypass default install process

notifications:
  irc:
    - "irc.freenode.org#i-mscp-dev"
    - "irc.freenode.org#i-mscp"
  email: false

script:
  - perlbrew exec --with 5.14,5.16,5.18,5.20,5.21 fakeroot perl ./test/perlunit.pl
