sudo: false

language: perl
perl:
  - "5.14"
  - "5.18"
  - "5.20"

before_install:
install:
  - cpanm -v --installdeps --notest --mirror http://cpan.mirrors.travis-ci.org .

notifications:
  irc: "irc.freenode.org#i-mscp-dev"
  email: false

script: perl ./engine/PerlLib/test/iMSCP/DirTests.pm
