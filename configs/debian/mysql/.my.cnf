[client]
host={DATABASE_HOST}
port={DATABASE_PORT}
user={DATABASE_USER}
password={DATABASE_PASSWORD}

[mysqldump]
host={DATABASE_HOST}
port={DATABASE_PORT}
user={DATABASE_USER}
password={DATABASE_PASSWORD}
