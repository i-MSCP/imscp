# Per distribution configuration parameters.
# Parameters defined in that file override the default that are defined in the
# master configuration file

# Set OS family statically as value reported by FACTER(8) is not consistent
DISTRO_FAMILY = Debian
