#!/bin/sh

# version: SVN: $Id$

umask 022

PHPRC="{IMSCP_FCGI_DIR}/{DOMAIN_NAME}/php"
TMPDIR="{TMP_DIR}"
PHP_FCGI_MAX_REQUEST=1000
PHP_FCGI_CHILDREN=0
export PHPRC TMPDIR PHP_FCGI_MAX_REQUEST PHP_FCGI_CHILDREN

exec {PHP_CGI_BIN}
